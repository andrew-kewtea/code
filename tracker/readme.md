# tracking

## models

task: progress:
task_cost(meta, extra):
 cost: time, cost, complexity,  
record  (userRecord) type: pos/loc, context
note: (txt)

log


@dataclass

vscode에서 uv 사용할떄 ctrl+shift+p로 select interpreter .venv/bin/python 해준다
uv init, uv add, ...select interpreter